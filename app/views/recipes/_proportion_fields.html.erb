<fieldset>
	<%= f.label :amount %>
	<%= f.select :amount, %w[1/4 1/2 3/4 1 1\ 1/4 1\ 1/2] %>
	<%= f.collection_select :measure_id, Measure.all, :id, :name %>
	<%= f.label :ingredient_id, "Ingredient" %>
	<%= f.collection_select :ingredient_id, Ingredient.all, :id, :name %>
	<%= f.check_box :_destroy %>
	<%= f.label :_destroy, "Remove Ingredient" %>
</fieldset>