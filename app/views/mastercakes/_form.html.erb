<%= form_for @mastercake do |f| %>
  <% if @mastercake.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mastercake.errors.count, "error") %> prohibited this mastercake from being saved:</h2>

      <ul>
      <% @mastercake.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <%= f.label :name %><br>
    <%= f.text_field :name %>

    <%= f.label :description %><br>
    <%= f.text_area :description %>

    <%= f.label :source %><br>
    <%= f.text_field :source %>

    <%= f.submit "Ready for Step 2", class: "medium round button"%>

<% end %>
